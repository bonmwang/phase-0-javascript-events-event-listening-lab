function addingEventListener() {
    const input = document.getElementById('button'); // Replace "button" with the actual ID of your button
    input.addEventListener('click', () => {
        alert('Button Clicked!');
    });
}